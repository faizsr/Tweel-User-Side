import 'package:flutter/material.dart';

class MessageIdleListView extends StatelessWidget {
  const MessageIdleListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}